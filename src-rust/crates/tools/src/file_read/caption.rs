// Caption builders — guarantees every block-returning handler ships a
// non-empty textual caption so `query/src/compact.rs::strip_images`
// substitutes our caption when blocks are flattened. Populated in
// commit 3 (PR B). Empty placeholder so commit 1 stays a pure relocation.
